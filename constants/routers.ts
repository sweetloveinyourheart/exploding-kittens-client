const HOME_ROUTER = "/"
const GUEST_REGISTER_ROUTER = "/guest"
const LOBBY_ROUTER = "/lobby"
const GAME_ROUTER = "/game"

export {
    HOME_ROUTER,
    GUEST_REGISTER_ROUTER,
    LOBBY_ROUTER,
    GAME_ROUTER,
}